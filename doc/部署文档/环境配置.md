# 环境配置文档

## 1. 配置概述

本系统采用多环境配置管理，支持开发环境（dev）、测试环境（test）、生产环境（prod）的配置隔离。

## 2. 后端配置

### 2.1 配置文件结构

```
xuanji-backend/src/main/resources/
├── application.yml              # 主配置文件
├── application-dev.yml         # 开发环境配置
├── application-test.yml        # 测试环境配置
└── application-prod.yml        # 生产环境配置
```

### 2.2 主配置文件 (application.yml)

```yaml
spring:
  profiles:
    active: dev  # 默认使用开发环境

server:
  port: 8080
  servlet:
    context-path: /api

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    root: info
    com.xuanji: debug
```

### 2.3 开发环境配置 (application-dev.yml)

```yaml
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xuanji_dev?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

jwt:
  secret: xuanji-dev-secret-key-1234567890123456
  expiration: 7200000  # 2小时

logging:
  level:
    com.xuanji: debug
    org.springframework.web: debug
  file:
    name: logs/xuanji-dev.log
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n'
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n'
```

### 2.4 测试环境配置 (application-test.yml)

```yaml
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://test-db-server:3306/xuanji_test?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: xuanji_test
    password: Xuanji@Test123
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  redis:
    host: test-redis-server
    port: 6379
    database: 1
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

jwt:
  secret: xuanji-test-secret-key-1234567890123456
  expiration: 7200000  # 2小时

logging:
  level:
    com.xuanji: info
  file:
    name: /opt/xuanji/logs/xuanji-test.log
  pattern:
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n'
```

### 2.5 生产环境配置 (application-prod.yml)

```yaml
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://prod-db-server:3306/xuanji?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=true}
    username: ${DB_USERNAME:xuanji}
    password: ${DB_PASSWORD:Xuanji@Prod123}
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  redis:
    host: ${REDIS_HOST:prod-redis-server}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: 0
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 20
        max-wait: -1ms
        max-idle: 10
        min-idle: 5

jwt:
  secret: ${JWT_SECRET:xuanji-prod-secret-key-1234567890123456}
  expiration: ${JWT_EXPIRATION:7200000}

logging:
  level:
    com.xuanji: warn
    org.springframework: warn
  file:
    name: /opt/xuanji/logs/xuanji-prod.log
  pattern:
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n'
  logback:
    rollingpolicy:
      max-file-size: 100MB
      max-history: 30
      total-size-cap: 10GB
```

### 2.6 环境变量配置

生产环境建议使用环境变量配置敏感信息：

```bash
# 数据库配置
export DB_URL=jdbc:mysql://prod-db-server:3306/xuanji?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=true
export DB_USERNAME=xuanji
export DB_PASSWORD=Xuanji@Prod123

# Redis 配置
export REDIS_HOST=prod-redis-server
export REDIS_PORT=6379
export REDIS_PASSWORD=your-redis-password

# JWT 配置
export JWT_SECRET=your-jwt-secret-key
export JWT_EXPIRATION=7200000
```

### 2.7 启动时指定环境

```bash
# 开发环境
java -jar xuanji-backend.jar --spring.profiles.active=dev

# 测试环境
java -jar xuanji-backend.jar --spring.profiles.active=test

# 生产环境
java -jar xuanji-backend.jar --spring.profiles.active=prod
```

## 3. 前端配置

### 3.1 环境配置文件

```
xuanji-frontend/
├── .env                    # 所有环境通用配置
├── .env.development        # 开发环境配置
├── .env.test               # 测试环境配置
└── .env.production         # 生产环境配置
```

### 3.2 通用配置 (.env)

```bash
# 应用标题
VUE_APP_TITLE=事务管理系统

# 应用版本
VUE_APP_VERSION=1.0.0

# 路由模式
VUE_APP_ROUTER_MODE=history
```

### 3.3 开发环境配置 (.env.development)

```bash
# 环境标识
NODE_ENV=development

# API 基础地址
VUE_APP_BASE_API=http://localhost:8080/api

# WebSocket 地址
VUE_APP_WS_API=ws://localhost:8080/ws

# 上传文件地址
VUE_APP_UPLOAD_API=http://localhost:8080/api/upload

# 是否启用 Mock 数据
VUE_APP_MOCK=false
```

### 3.4 测试环境配置 (.env.test)

```bash
# 环境标识
NODE_ENV=production

# API 基础地址
VUE_APP_BASE_API=http://test-api-server:8080/api

# WebSocket 地址
VUE_APP_WS_API=ws://test-api-server:8080/ws

# 上传文件地址
VUE_APP_UPLOAD_API=http://test-api-server:8080/api/upload

# 是否启用 Mock 数据
VUE_APP_MOCK=false
```

### 3.5 生产环境配置 (.env.production)

```bash
# 环境标识
NODE_ENV=production

# API 基础地址
VUE_APP_BASE_API=/api

# WebSocket 地址
VUE_APP_WS_API=wss://your-domain.com/ws

# 上传文件地址
VUE_APP_UPLOAD_API=/api/upload

# 是否启用 Mock 数据
VUE_APP_MOCK=false
```

### 3.6 构建命令

```json
{
  "scripts": {
    "dev": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "build:test": "vue-cli-service build --mode test",
    "build:prod": "vue-cli-service build --mode production",
    "lint": "vue-cli-service lint"
  }
}
```

### 3.7 Axios 配置示例

```javascript
// src/utils/request.js
import axios from 'axios'
import { Message } from 'element-ui'

const service = axios.create({
  baseURL: process.env.VUE_APP_BASE_API,
  timeout: 15000
})

service.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers['Authorization'] = 'Bearer ' + token
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

service.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      Message.error(res.message || 'Error')
      return Promise.reject(new Error(res.message || 'Error'))
    } else {
      return res
    }
  },
  error => {
    Message.error(error.message)
    return Promise.reject(error)
  }
)

export default service
```

## 4. 数据库配置

### 4.1 数据库初始化脚本

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS xuanji DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 使用数据库
USE xuanji;

-- 创建用户表
CREATE TABLE sys_user (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '用户ID',
  username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
  password VARCHAR(100) NOT NULL COMMENT '密码',
  real_name VARCHAR(50) COMMENT '真实姓名',
  phone VARCHAR(20) COMMENT '手机号',
  email VARCHAR(100) COMMENT '邮箱',
  org_id BIGINT COMMENT '所属机构ID',
  status TINYINT DEFAULT 1 COMMENT '状态：0-禁用，1-启用',
  deleted TINYINT DEFAULT 0 COMMENT '删除标记：0-未删除，1-已删除',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_username (username),
  INDEX idx_org_id (org_id),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 创建机构表
CREATE TABLE sys_org (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '机构ID',
  org_name VARCHAR(100) NOT NULL COMMENT '机构名称',
  org_code VARCHAR(50) NOT NULL UNIQUE COMMENT '机构编码',
  parent_id BIGINT DEFAULT 0 COMMENT '父机构ID',
  org_level INT DEFAULT 1 COMMENT '机构层级',
  sort INT DEFAULT 0 COMMENT '排序号',
  status TINYINT DEFAULT 1 COMMENT '状态：0-禁用，1-启用',
  deleted TINYINT DEFAULT 0 COMMENT '删除标记：0-未删除，1-已删除',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_org_code (org_code),
  INDEX idx_parent_id (parent_id),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='机构表';

-- 创建角色表
CREATE TABLE sys_role (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '角色ID',
  role_name VARCHAR(50) NOT NULL COMMENT '角色名称',
  role_code VARCHAR(50) NOT NULL UNIQUE COMMENT '角色编码',
  description VARCHAR(200) COMMENT '角色描述',
  status TINYINT DEFAULT 1 COMMENT '状态：0-禁用，1-启用',
  deleted TINYINT DEFAULT 0 COMMENT '删除标记：0-未删除，1-已删除',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_role_code (role_code),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- 创建菜单表
CREATE TABLE sys_menu (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '菜单ID',
  menu_name VARCHAR(50) NOT NULL COMMENT '菜单名称',
  menu_type TINYINT NOT NULL COMMENT '菜单类型：0-目录，1-菜单，2-按钮',
  parent_id BIGINT DEFAULT 0 COMMENT '父菜单ID',
  path VARCHAR(200) COMMENT '路由路径',
  component VARCHAR(200) COMMENT '组件路径',
  icon VARCHAR(100) COMMENT '菜单图标',
  permission VARCHAR(100) COMMENT '权限标识',
  sort INT DEFAULT 0 COMMENT '排序号',
  status TINYINT DEFAULT 1 COMMENT '状态：0-禁用，1-启用',
  deleted TINYINT DEFAULT 0 COMMENT '删除标记：0-未删除，1-已删除',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_parent_id (parent_id),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单表';

-- 创建用户角色关联表
CREATE TABLE sys_user_role (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '关联ID',
  user_id BIGINT NOT NULL COMMENT '用户ID',
  role_id BIGINT NOT NULL COMMENT '角色ID',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  UNIQUE KEY uk_user_role (user_id, role_id),
  INDEX idx_user_id (user_id),
  INDEX idx_role_id (role_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';

-- 创建角色菜单关联表
CREATE TABLE sys_role_menu (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '关联ID',
  role_id BIGINT NOT NULL COMMENT '角色ID',
  menu_id BIGINT NOT NULL COMMENT '菜单ID',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  UNIQUE KEY uk_role_menu (role_id, menu_id),
  INDEX idx_role_id (role_id),
  INDEX idx_menu_id (menu_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';

-- 插入初始数据
-- 插入默认管理员用户（密码：admin123，BCrypt加密后）
INSERT INTO sys_user (username, password, real_name, phone, email, org_id, status) VALUES
('admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iAt6Z5EH', '系统管理员', '13800138000', 'admin@example.com', 1, 1);

-- 插入默认机构
INSERT INTO sys_org (org_name, org_code, parent_id, org_level, sort, status) VALUES
('总部', 'ROOT', 0, 1, 1, 1),
('技术部', 'TECH', 1, 2, 1, 1),
('市场部', 'MARKET', 1, 2, 2, 1);

-- 插入默认角色
INSERT INTO sys_role (role_name, role_code, description, status) VALUES
('超级管理员', 'SUPER_ADMIN', '拥有系统所有权限', 1),
('普通用户', 'USER', '普通用户角色', 1);

-- 插入默认菜单
INSERT INTO sys_menu (menu_name, menu_type, parent_id, path, component, icon, permission, sort, status) VALUES
('系统管理', 0, 0, '/system', 'Layout', 'el-icon-setting', '', 1, 1),
('用户管理', 1, 1, '/system/user', 'system/user/index', 'el-icon-user', 'system:user:list', 1, 1),
('机构管理', 1, 1, '/system/org', 'system/org/index', 'el-icon-office-building', 'system:org:list', 2, 1),
('角色管理', 1, 1, '/system/role', 'system/role/index', 'el-icon-s-custom', 'system:role:list', 3, 1),
('菜单管理', 1, 1, '/system/menu', 'system/menu/index', 'el-icon-menu', 'system:menu:list', 4, 1);

-- 为超级管理员分配所有菜单
INSERT INTO sys_role_menu (role_id, menu_id)
SELECT 1, id FROM sys_menu;

-- 为管理员分配超级管理员角色
INSERT INTO sys_user_role (user_id, role_id) VALUES (1, 1);
```

### 4.2 数据库连接池配置

```yaml
spring:
  datasource:
    hikari:
      # 最大连接数
      maximum-pool-size: 50
      # 最小空闲连接数
      minimum-idle: 10
      # 连接超时时间（毫秒）
      connection-timeout: 30000
      # 空闲连接超时时间（毫秒）
      idle-timeout: 600000
      # 连接最大生命周期（毫秒）
      max-lifetime: 1800000
      # 连接测试查询
      connection-test-query: SELECT 1
```

## 5. Nginx 配置

### 5.1 开发环境配置

```nginx
server {
    listen 80;
    server_name localhost;

    location / {
        root /path/to/xuanji-frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://localhost:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### 5.2 生产环境配置

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /opt/xuanji/frontend;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 反向代理到后端
    location /api/ {
        proxy_pass http://backend-server:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # 缓冲设置
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
    }

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
}
```

## 6. Docker 配置（可选）

### 6.1 后端 Dockerfile

```dockerfile
FROM openjdk:8-jdk-alpine

WORKDIR /app

COPY target/xuanji-backend-1.0.0.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]
```

### 6.2 前端 Dockerfile

```dockerfile
FROM node:14-alpine as build

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html

COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

### 6.3 Docker Compose

```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: xuanji-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: xuanji
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - xuanji-network

  redis:
    image: redis:6.2
    container_name: xuanji-redis
    ports:
      - "6379:6379"
    networks:
      - xuanji-network

  backend:
    build:
      context: ./xuanji-backend
      dockerfile: Dockerfile
    container_name: xuanji-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_URL: jdbc:mysql://mysql:3306/xuanji?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
      DB_USERNAME: root
      DB_PASSWORD: root
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - mysql
      - redis
    networks:
      - xuanji-network

  frontend:
    build:
      context: ./xuanji-frontend
      dockerfile: Dockerfile
    container_name: xuanji-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - xuanji-network

volumes:
  mysql-data:

networks:
  xuanji-network:
    driver: bridge
```

## 7. 配置检查清单

### 7.1 后端部署前检查

- [ ] 确认 JDK 版本（JDK 8 或 11）
- [ ] 确认 MySQL 版本（8.0+）
- [ ] 确认数据库连接信息正确
- [ ] 确认 Redis 连接信息正确（如使用）
- [ ] 确认 JWT 密钥已设置
- [ ] 确认日志目录有写权限
- [ ] 确认防火墙端口已开放
- [ ] 确认环境变量已配置（生产环境）

### 7.2 前端部署前检查

- [ ] 确认 Node.js 版本（14.0+）
- [ ] 确认 API 地址配置正确
- [ ] 确认构建环境正确（dev/test/prod）
- [ ] 确认 Nginx 配置正确
- [ ] 确认静态文件路径正确
- [ ] 确认反向代理配置正确
- [ ] 确认防火墙端口已开放

### 7.3 数据库部署前检查

- [ ] 确认 MySQL 版本（8.0+）
- [ ] 确认数据库已创建
- [ ] 确认用户已创建并授权
- [ ] 确认表结构已创建
- [ ] 确认初始数据已导入
- [ ] 确认字符集配置正确（utf8mb4）
- [ ] 确认防火墙端口已开放

## 8. 配置最佳实践

### 8.1 安全性

- 生产环境不要在配置文件中硬编码敏感信息
- 使用环境变量或配置中心管理敏感信息
- 定期更换 JWT 密钥和数据库密码
- 启用 SSL/TLS 加密传输
- 配置防火墙规则，限制访问

### 8.2 性能优化

- 合理配置数据库连接池大小
- 启用 Redis 缓存（如需要）
- 配置 Nginx Gzip 压缩
- 配置静态资源缓存
- 使用 CDN 加速静态资源（生产环境）

### 8.3 可维护性

- 使用多环境配置隔离
- 配置文件添加详细注释
- 使用版本控制管理配置文件
- 记录配置变更历史
- 定期备份配置文件

### 8.4 可移植性

- 使用相对路径而非绝对路径
- 配置外部化，支持环境变量覆盖
- 使用 Docker 容器化部署
- 提供详细的部署文档
- 提供配置检查脚本

---

**文档版本：** v1.0
**创建日期：** 2026-02-25
**最后更新：** 2026-02-25
